import React from 'react';
import { withRouter, Link } from 'react-router-dom';

class ActivityIndex extends React.Component {
  render() {
    return (
      <h1>Antes muerto que sencillo</h1>
    );
  }
}

export default withRouter(ActivityIndex);
